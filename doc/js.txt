                                                                   *Js.toggle()*
                                 `Js.toggle`()
Toggle the plugin by calling the `enable`/`disable` methods respectively.

------------------------------------------------------------------------------
                                                                   *Js.enable()*
                              `Js.enable`({scope})
Initializes the plugin, sets event listeners and internal state.

------------------------------------------------------------------------------
                                                                  *Js.disable()*
                                 `Js.disable`()
Disables the plugin, clear highlight groups and autocmds, closes side buffers and resets the internal state.


==============================================================================
------------------------------------------------------------------------------
                                                                    *Js.options*
                                  `Js.options`
Js configuration with its default values.

Type ~
`(table)`
Default values:
>lua
  Js.options = {
      -- Prints useful logs about what event are triggered, and reasons actions are executed.
      debug = false,
  }

<
------------------------------------------------------------------------------
                                                                    *Js.setup()*
                             `Js.setup`({options})
Define your js setup.

Parameters ~
{options} `(table)` Module config table. See |Js.options|.

Usage ~
`require("js").setup()` (add `{}` with your |Js.options| table)


==============================================================================
------------------------------------------------------------------------------
Class ~
{TSNode}
Fields ~
{range} `(fun(self: TSNode): number,)` number, number, number
{start} `(fun(self: TSNode): number,)` number
{type} `(fun(self: TSNode): string)`
{field} `(fun(self: TSNode, name: string): TSNode[])`
{parent} `(fun(self: TSNode): TSNode|nil)`
{named_child} `(fun(self: TSNode, index: number): TSNode|nil)`
{named_child_count} `(fun(self: TSNode): number)`
{iter_children} `(fun(self: TSNode): fun():)` TSNode, string


==============================================================================
------------------------------------------------------------------------------
                                                                     *M.setup()*
                              `M.setup`({null_ls})
Parameters ~
{null_ls} `(table)` The null-ls module

------------------------------------------------------------------------------
                                                               *M.get_sources()*
                           `M.get_sources`({null_ls})
Parameters ~
{null_ls} `(table)` The null-ls module
Return ~
`(table[])` List of source definitions


==============================================================================
------------------------------------------------------------------------------
                                                         *M.can_remove_braces()*
                  `M.can_remove_braces`({bufnr}, {row}, {col})
Parameters ~
{bufnr} `(number)` Buffer number
{row} `(number)` 0-indexed row
{col} `(number)` 0-indexed column
Return ~
`(boolean)`, TSNode|nil, TSNode|nil Returns can_transform, expression node, arrow_function node

------------------------------------------------------------------------------
                                                 *M.create_brace_removal_edit()*
       `M.create_brace_removal_edit`({arrow_node}, {expr_node}, {bufnr})
Parameters ~
{arrow_node} `(TSNode)` The arrow_function node
{expr_node} `(TSNode)` The expression node
{bufnr} `(number)` Buffer number
Return ~
`(string|nil)` The new arrow function text


==============================================================================
------------------------------------------------------------------------------
                                                      *M.can_convert_to_arrow()*
                `M.can_convert_to_arrow`({bufnr}, {row}, {col})
Parameters ~
{bufnr} `(number)` Buffer number
{row} `(number)` 0-indexed row
{col} `(number)` 0-indexed column
Return ~
`(boolean)`, TSNode|nil Returns can_convert, function_node

------------------------------------------------------------------------------
                                              *M.create_arrow_conversion_edit()*
           `M.create_arrow_conversion_edit`({function_node}, {bufnr})
Parameters ~
{function_node} `(TSNode)` The function node
{bufnr} `(number)` Buffer number
Return ~
`(string|nil)` The new arrow function text


==============================================================================
------------------------------------------------------------------------------
                                                     *M.show_replacement_menu()*
           `M.show_replacement_menu`({occurrence_count}, {callback})
Show replacement menu using vim.ui.select
Parameters ~
{occurrence_count} `(number)`
{callback} `(function)`(choice: string|nil)

------------------------------------------------------------------------------
                                                      *M.prompt_variable_name()*
          `M.prompt_variable_name`({scope_node}, {bufnr}, {callback})
Prompt for variable name with validation loop
Parameters ~
{scope_node} `(TSNode)`
{bufnr} `(number)`
{callback} `(function)`(var_name: string|nil)

------------------------------------------------------------------------------
                                                   *M.apply_multi_replacement()*
  `M.apply_multi_replacement`({occurrences}, {var_name}, {expr_text}, {bufnr})
Apply multi-occurrence replacement
Parameters ~
{occurrences} `(table[])` Array of {node, row, col, end_row, end_col}
{var_name} `(string)`
{expr_text} `(string)` Original expression text
{bufnr} `(number)`


==============================================================================
------------------------------------------------------------------------------
                                                      *M.find_enclosing_scope()*
                        `M.find_enclosing_scope`({node})
Parameters ~
{node} `(TSNode)`
Return ~
`(TSNode)`

------------------------------------------------------------------------------
                                                 *M.normalize_expression_text()*
                 `M.normalize_expression_text`({node}, {bufnr})
Normalize expression text (collapse whitespace)
Parameters ~
{node} `(TSNode)`
{bufnr} `(number)`
Return ~
`(string)`

------------------------------------------------------------------------------
                                                         *M.expressions_equal()*
                `M.expressions_equal`({node1}, {node2}, {bufnr})
Compare two nodes for equality (type + normalized text recursively)
Parameters ~
{node1} `(TSNode)`
{node2} `(TSNode)`
{bufnr} `(number)`
Return ~
`(boolean)`

------------------------------------------------------------------------------
                                                *M.find_identical_expressions()*
       `M.find_identical_expressions`({expr_node}, {scope_node}, {bufnr})
Find all identical expressions in scope
Parameters ~
{expr_node} `(TSNode)` expression to match
{scope_node} `(TSNode)` scope to search in
{bufnr} `(number)`
Return ~
`(table[])` Array of {node, row, col, end_row, end_col}

------------------------------------------------------------------------------
                                                *M.find_const_insertion_point()*
                  `M.find_const_insertion_point`({scope_node})
Find where to insert const declaration
Parameters ~
{scope_node} `(TSNode)`
Return ~
`(number)` row, number col

------------------------------------------------------------------------------
                                                     *M.check_variable_exists()*
          `M.check_variable_exists`({scope_node}, {var_name}, {bufnr})
Check if variable exists in scope
Parameters ~
{scope_node} `(TSNode)`
{var_name} `(string)`
{bufnr} `(number)`
Return ~
`(boolean)`

------------------------------------------------------------------------------
                                                         *M.find_common_scope()*
                         `M.find_common_scope`({nodes})
Find common ancestor scope for multiple nodes
Parameters ~
{nodes} `(TSNode[])`
Return ~
`(TSNode)`


 vim:tw=78:ts=8:noet:ft=help:norl: